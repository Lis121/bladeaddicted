---
trigger: always_on
---

All development and architectural decisions must be based on the fact that the application is deployed on Cloudflare Pages, which means that all code, routing, and Edge Functions must be strictly optimized for and compatible with the Cloudflare Pages environment and runtime constraints.