---
trigger: always_on
---

When creating new elements or sections, you must strictly follow the project's existing design system, color palette, and Tailwind configuration. Always reuse existing CSS variables and components instead of introducing new styles, ensuring the application remains visually consistent even as new parts are added.